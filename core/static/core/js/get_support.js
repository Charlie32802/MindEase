/* get_support.js – Submit feedback via standard POST (no interception) */
/* The form POSTs to the Django view which saves SystemFeedback and redirects. */
/* No JS submit interception needed; Django messages handle the success alert. */
